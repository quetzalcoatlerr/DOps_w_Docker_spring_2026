services:
  front:
    build: ./frontend
    ports:
      - 5001:5001
  back:
    build: ./backend
    ports:
      - 8080:8080
    environment:
      - REQUEST_ORIGIN=http://localhost:5001
      - REDIS_HOST=redis
  redis:
    image: redis:latest
